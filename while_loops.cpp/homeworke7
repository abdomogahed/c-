#include<iostream>
using namespace std;

int main() {
	int n;
	int x = 0;
	cout << "entr 1 num \n";
	cin >> n;
	while (x <= n) {
		if (x % 8 == 0 || (x % 3 == 0&&x%4==0)) {
			cout << x<<" ";
		}
		x = x + 1;

	}

	return 0;

}
