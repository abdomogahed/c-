#include<iostream>
using namespace std;

int main() {
	int n, t, r=1, y=1;

	cout << "enter 2 num \n";

	cin >> n>>t;
	
	while (y < t+1) {
		cout << r << "*" << y << "=" << r * y << "\n";
		y++;
		if (y == t+1) {
			r++;
			y = 1;
		}
		if (r == n+1)y = t+1;
		    
	}



	return 0;
}
