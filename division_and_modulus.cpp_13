#include <iostream>
using namespace std;
int main()
{

    int n ,s1 , e1 , s2 , e2 , s3 , e3;
    int m = 0;
    cout << "enter 1 num\n";
    cin >> n;
    cout << "enter 6 num \n";
    cin >> s1 >> e1 >> s2 >> e2 >> s3 >> e3;
    if (n > s1 && n < e1)m = m + 1;
    else if (n < s1 && n > e1)m = m + 1;

    if (n > s2 && n < e2)m = m + 1;
    else if (n < s2 && n > e2)m = m + 1;

    if (n > s3 && n < e3)m = m + 1;
    else if (n < s3 && n > e3)m = m + 1;
    cout << m;

    


    return 0;
}
*****************************************************************
#include<iostream>
using namespace std;

int main() {
	int x, start, end, cnt = 0;

	cin>>x;

	//Read start and end, then see if X is between them or not, 3 times
	cin>>start>>end;
	cnt += (start <= x && x <= end);

	cin>>start>>end;
	cnt += (start <= x && x <= end);

	cin>>start>>end;
	cnt += (start <= x && x <= end);

	cout<<cnt<<"\n";

	return 0;
}
